# gitleaks false positives, suppressed by fingerprint (commit:file:rule:line).
#
# The marketing "surfaces" tabs render a SAMPLE captured event, and a real captured event carries a
# `dedupKey` — a provider-supplied idempotency key. gitleaks' generic-api-key rule fires on ANY value
# bound to a name containing "key", so it flags the demo values on entropy alone. They are made-up
# illustrative values in public marketing copy, not credentials, and the field cannot be renamed —
# `dedupKey` is the real API field name the tab is showing.
#
# The CURRENT file suppresses these inline (`// gitleaks:allow`). These fingerprints cover the same
# values in git HISTORY, which gitleaks also scans and which cannot be fixed at the source.
#
# ⚠️ Fingerprints are COMMIT-BOUND: a rebase rewrites the SHA and they stop matching. If the secret
# scan starts failing on these lines again after a rebase, re-derive them from the run log rather
# than adding a broader rule.
5314c00f8792ddeeac5aa2a5ae00313020aab210:apps/www/src/components/marketing/surfaces/surfaces-tabs.tsx:generic-api-key:63
5314c00f8792ddeeac5aa2a5ae00313020aab210:apps/www/src/components/marketing/surfaces/surfaces-tabs.tsx:generic-api-key:74
5314c00f8792ddeeac5aa2a5ae00313020aab210:apps/www/src/components/marketing/surfaces/surfaces-tabs.tsx:generic-api-key:85
# The same three values under their PRE-REBASE commit (from #515, merged to main before this branch
# was rebased). Kept so the ignore is correct from either history.
71bb17019c67e56c96d4bc2e433bd79c7a014260:apps/www/src/components/marketing/surfaces/surfaces-tabs.tsx:generic-api-key:63
71bb17019c67e56c96d4bc2e433bd79c7a014260:apps/www/src/components/marketing/surfaces/surfaces-tabs.tsx:generic-api-key:74
71bb17019c67e56c96d4bc2e433bd79c7a014260:apps/www/src/components/marketing/surfaces/surfaces-tabs.tsx:generic-api-key:85

# The invite-cookie unit test's FIXTURE token. `sealInvitePayload({ org, token }, SECRET, T0)` — the
# test's own secret is the literal string "test-secret-abc" and the token is a made-up value; the
# generic-api-key rule fired on the `token:` name plus entropy alone. Nothing here is a credential.
#
# The CURRENT tree is now de-entropied (the token is `whinv_example_test_value`, which the rule does not
# flag), so no inline suppression is needed going forward and a line-shift can't mint a new fingerprint.
# What remains is the FROZEN occurrence in history, which gitleaks still walks and which cannot be rewritten.
# The reported commit depends on which refs are reachable in a given checkout: a fresh PR checkout sees the
# squash of #587 into main (ab468a7); a local clone that still has the pre-merge feature-branch commits sees
# those (8616490 for the test, ec7c840 for the plan doc). Both sets are listed so the ignore is correct from
# either history — the same belt-and-suspenders as the surfaces-tabs entries above.
ab468a7ab46bceb7503a4d2f1bb823bd58b11148:apps/web/src/server/invite-cookie.test.ts:generic-api-key:12
ab468a7ab46bceb7503a4d2f1bb823bd58b11148:apps/web/src/server/invite-cookie.test.ts:generic-api-key:15
8616490897d9d74f1a1d6b9f965a13606804ac26:apps/web/src/server/invite-cookie.test.ts:generic-api-key:12
8616490897d9d74f1a1d6b9f965a13606804ac26:apps/web/src/server/invite-cookie.test.ts:generic-api-key:15
# ...and the plan document, which simply quotes that same test's code block verbatim.
ab468a7ab46bceb7503a4d2f1bb823bd58b11148:docs/superpowers/plans/2026-07-14-invite-return-through-login.md:generic-api-key:136
ab468a7ab46bceb7503a4d2f1bb823bd58b11148:docs/superpowers/plans/2026-07-14-invite-return-through-login.md:generic-api-key:139
ec7c840f1a1dd0022714bd51b2d05009f34ac39d:docs/superpowers/plans/2026-07-14-invite-return-through-login.md:generic-api-key:136
ec7c840f1a1dd0022714bd51b2d05009f34ac39d:docs/superpowers/plans/2026-07-14-invite-return-through-login.md:generic-api-key:139

# The IndexNow key (scripts/indexnow-submit.mjs). generic-api-key fires on the name "KEY" plus entropy
# alone. This value is PUBLISHED by design at `https://<host>/<key>.txt` on every host we submit for —
# the protocol requires it to be world-readable so search engines can verify domain ownership. A
# scanner finding it in the repo has found nothing that is not already served to the open internet,
# and it grants nothing beyond "may submit URLs for this host".
#
# The CURRENT tree suppresses it inline (`// gitleaks:allow`), so this fingerprint covers only the
# FROZEN occurrence in this branch's history — the commit that added the line before the inline
# suppression existed.
df16cdf1f536403d8ccc770df927d0b15ed35b3e:scripts/indexnow-submit.mjs:generic-api-key:31
