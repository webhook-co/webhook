// Shared vitest environment setup for @webhook-co/video, mirroring
// packages/ui/vitest.setup.ts. Empty for now — no component tests exist yet
// (Task 5 adds real Remotion/React component tests). Extend here with jsdom
// shims or custom matchers (e.g. `@testing-library/jest-dom/vitest`) once
// those tests land and need them.
export {};
