import { SERVICE_NAME } from "@webhook-co/shared";

// Placeholder CLI entrypoint. The TypeScript/Bun CLI (listen / replay-to-localhost) lands here.
export const cliName = `${SERVICE_NAME}` as const;
