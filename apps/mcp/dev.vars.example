# apps/mcp/dev.vars.example — COMMITTED reference, regenerated by `pnpm dev:secrets --examples`.
# Run `pnpm dev:secrets` to write the real apps/mcp/.dev.vars with generated values.
# Never paste a secret into this file: it is committed, and this repo is public.
# Key names and their meaning live in scripts/dev-secrets-manifest.mjs.

# GENERATED: HMAC pepper for API keys and ingest tokens. Read by api, engine, mcp, auth, web and packages/db — a mismatch makes a key minted by one surface unrecognisable to another.
CREDENTIAL_PEPPER=

# GENERATED: Hash-chain key for the tamper-evident audit log. Must match across every surface that appends or verifies.
AUDIT_CHAIN_HMAC_KEY=

# GENERATED: HMAC key for keyset pagination cursors. Read by api, engine and mcp; a cursor issued by one must verify in another.
CURSOR_KEY=

# LOCAL: See api. Must match api's value and the local engine's port, or the MCP tool hands out a URL nothing is listening on.
INGEST_BASE_URL=http://localhost:8787

# LOCAL: See api.
FREE_EVENT_CAP=5000

# LOCAL: The plugin-directory domain-verification token served at /.well-known/openai-apps-challenge (ADR-0132). A local value so the route is EXERCISABLE locally rather than dark — curl it and you should get exactly this string back. The committed var is the deploy-time placeholder <OPENAI_APPS_CHALLENGE_TOKEN>, which is non-empty, so without a local override the endpoint would have served the literal placeholder as the token; the handler now rejects any /^<[A-Z0-9_]+>$/ value as well.
OPENAI_APPS_CHALLENGE_TOKEN=local-openai-apps-challenge-token

# GENERATED: Signs the principal-bound Mcp-Session-Id. No dev fallback — missing it surfaces as an anonymous TypeError from readSecretBinding, not a named error.
MCP_SESSION_KEY=
