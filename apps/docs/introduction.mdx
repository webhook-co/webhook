---
title: "Introduction"
description: "A free, permanent, signed webhook URL — inspect every request, replay it to localhost, and deliver it onward. The same from the CLI, API, dashboard, and MCP."
---

webhook.co gives you a **free, permanent, signed webhook URL** with payload inspection and one-command replay-to-localhost — then grows with you into inbound ingestion (verify → [dedup](/deduplication) → retry → replay) and outbound delivery. Every capability is reachable identically across the **CLI, API, dashboard, and MCP**, and nothing is public unless you make it so.

Signing and verification follow the [Standard Webhooks](https://www.standardwebhooks.com/) spec, for both send and receive.

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Create a signed URL, send a test event, and watch it arrive — in a few minutes.
  </Card>

  <Card title="How it works" icon="diagram-project" href="/concepts/how-webhook-co-works">
    The path a webhook takes: ingest → verify → dedup → store → deliver → replay.
  </Card>
</CardGroup>

## Explore

<CardGroup cols={2}>
  <Card title="Ingest" icon="inbox-in" href="/concepts/ingest-urls">
    A permanent, signed URL on a dedicated apex that accepts every request and never expires.
  </Card>

<Card title="Verify" icon="shield-check" href="/guides/verify-inbound-signatures">
  Register a provider secret and webhook.co verifies every inbound request — 142 providers built in.
</Card>

<Card title="Deliver" icon="paper-plane" href="/concepts/delivery-retry-signing">
  Forward events to a destination with at-least-once delivery, bounded retries, and outbound
  signing.
</Card>

  <Card title="Trigger an agent" icon="robot" href="/mcp/wake-an-agent">
    Wake an MCP agent the moment a webhook lands — an inbound trigger for the agent era.
  </Card>
</CardGroup>

## The API

The REST API lives at `https://api.webhook.co`. It's a small, predictable surface — every endpoint is described by a machine-readable OpenAPI 3.1 document you can browse in the [**API reference**](/api-reference/endpoints/list-endpoints) or fetch yourself:

```sh
curl https://api.webhook.co/openapi.json
```

That same document generates the SDKs and this site's API reference, so they never drift from the real API.

## SDKs

Official, typed SDKs in three languages — each with the same hardening built in (bearer auth, bounded retries with jitter, cursor pagination, idempotency, and secret redaction):

<CardGroup cols={3}>
  <Card title="TypeScript" href="/sdks/typescript">
    `npm install @webhook-co/sdk`
  </Card>

<Card title="Python" href="/sdks/python">
  `pip install webhook-co`
</Card>

  <Card title="Go" href="/sdks/go">
    `go get github.com/webhook-co/webhook-go`
  </Card>
</CardGroup>

## Built for agents, too

These docs are readable by your coding agent. Point it at [`docs.webhook.co/mcp`](/mcp/connect-the-docs-mcp) to search them over MCP, or use the **Copy page** menu to drop any page into ChatGPT, Claude, or Cursor.
