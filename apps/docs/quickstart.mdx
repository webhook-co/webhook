---
title: "Quickstart"
sidebarTitle: "Quickstart"
description: "Create a signed ingest URL, send it a test event, and watch it arrive as a captured event — in a few minutes."
icon: "rocket"
---

Create an endpoint, send its ingest URL a request, and see the captured event — in a few minutes. No provider setup, no receiver to stand up.

<Steps>
  <Step title="Get an API key">
    Create a `whk_` API key in your [dashboard](https://app.webhook.co) under **Settings → Credentials**, then export it:

    ```sh
    export WEBHOOK_API_KEY=whk_your_key_here
    ```

    Prefer not to manage a key? `wbhk login` signs the CLI in over OAuth — see [authentication](/authentication#signing-in-from-the-cli).

  </Step>

  <Step title="Install an SDK">
    <CodeGroup>

    ```sh npm
    npm install @webhook-co/sdk
    ```

    ```sh pip
    pip install webhook-co
    ```

    ```sh go
    go get github.com/webhook-co/webhook-go
    ```

    </CodeGroup>

  </Step>

  <Step title="Create an endpoint">
    Each endpoint has a permanent, signed **ingest URL** on `wbhk.my`. It's returned here and always visible (and re-revealable) in your dashboard — it never expires, and changes only if you rotate it.

    <CodeGroup>

    ```ts TypeScript
    import { WebhookClient } from "@webhook-co/sdk";

    const webhook = new WebhookClient({ apiKey: process.env.WEBHOOK_API_KEY! });
    const endpoint = await webhook.endpoints.create({ name: "orders-prod" });
    console.log(endpoint.ingestUrl); // https://wbhk.my/whep_...
    ```

    ```python Python
    import os
    from webhook_co import WebhookClient

    client = WebhookClient(api_key=os.environ["WEBHOOK_API_KEY"])
    endpoint = client.endpoints.create(name="orders-prod")
    print(endpoint.ingest_url)  # https://wbhk.my/whep_...
    ```

    ```go Go
    client, _ := webhook.NewClient(os.Getenv("WEBHOOK_API_KEY"))
    ep, _ := client.Endpoints.Create(ctx, webhook.EndpointsCreateParams{Name: "orders-prod"})
    fmt.Println(ep.IngestUrl) // https://wbhk.my/whep_...
    ```

    ```sh CLI
    wbhk endpoints create orders-prod
    # prints the ingest URL: https://wbhk.my/whep_...
    ```

    </CodeGroup>

  </Step>

  <Step title="Send it a test event">
    Point a provider at the ingest URL, or just `POST` to it yourself — no key required to _send_. The ingest URL accepts every verb; a write is captured and acknowledged with `200`.

    ```sh
    curl -X POST https://wbhk.my/whep_your_token \
      -H "content-type: application/json" \
      -d '{"hello":"webhook.co"}'
    # → 200
    ```

  </Step>

  <Step title="Watch it arrive">
    The request is now a captured **event**. List the endpoint's events — the list auto-paginates over the cursor:

    <CodeGroup>

    ```ts TypeScript
    for await (const event of webhook.events.list(endpoint.id)) {
      console.log(event.id, event.provider, event.verificationState);
    }
    ```

    ```python Python
    for event in client.events.list(endpoint.id):
        print(event.id, event.provider, event.verification_state)
    ```

    ```sh CLI
    wbhk events list <endpoint-id>
    ```

    </CodeGroup>

    You'll see your `{"hello":"webhook.co"}` event with `verificationState: unattempted` — captured, but unsigned until you [register a provider secret](/guides/verify-inbound-signatures). That's the whole loop: land, capture, inspect.

  </Step>
</Steps>

## Next

<CardGroup cols={2}>
  <Card title="Verify inbound signatures" icon="shield-check" href="/guides/verify-inbound-signatures">
    Register a provider secret so events arrive `verified`.
  </Card>

  <Card title="Replay to localhost" icon="laptop-code" href="/guides/replay-to-localhost">
    Forward captured events to your machine with one command.
  </Card>
</CardGroup>
