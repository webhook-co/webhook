{
  "name": "@webhook-co/dmarc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled --port 8793 --ip 127.0.0.1 --inspector-port 9793",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run && vitest run --config vitest.workers.config.ts"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.1",
    "@cloudflare/workers-types": "^5.20260801.1"
  },
  "dependencies": {
    "fast-xml-parser": "^5.10.1",
    "postal-mime": "^2.7.5"
  },
  "main": "src/index.ts"
}
