# apps/health/dev.vars.example — COMMITTED reference, regenerated by `pnpm dev:secrets --examples`.
# Run `pnpm dev:secrets` to write the real apps/health/.dev.vars with generated values.
# Never paste a secret into this file: it is committed, and this repo is public.
# Key names and their meaning live in scripts/dev-secrets-manifest.mjs.

# GENERATED: Authenticates the heartbeat POST. Generated per machine: it is a shared secret with the caller, and locally you are both ends of it.
HEARTBEAT_TOKEN=
