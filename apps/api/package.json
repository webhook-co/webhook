{
  "name": "@webhook-co/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev -c wrangler.dev.jsonc --test-scheduled --port 8788 --ip 127.0.0.1 --inspector-port 9788",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage",
    "test:db": "vitest run --config vitest.pg.config.ts"
  },
  "dependencies": {
    "@webhook-co/contract": "workspace:*",
    "@webhook-co/db": "workspace:*",
    "@webhook-co/openapi": "workspace:*",
    "@webhook-co/shared": "workspace:*",
    "@webhook-co/webhooks-spec": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1"
  }
}
