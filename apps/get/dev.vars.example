# apps/get/dev.vars.example — COMMITTED reference, regenerated by `pnpm dev:secrets --examples`.
# Run `pnpm dev:secrets` to write the real apps/get/.dev.vars with generated values.
# Never paste a secret into this file: it is committed, and this repo is public.
# Key names and their meaning live in scripts/dev-secrets-manifest.mjs.
